import { SearchbikePipe } from './searchbike.pipe';

describe('SearchbikePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbikePipe();
    expect(pipe).toBeTruthy();
  });
});
